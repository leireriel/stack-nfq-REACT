import './styles.scss';
import React from 'react';

const QuestionDetails = props => {
  return <div className="question__details-page">question details: {props.id}</div>;
};

export default QuestionDetails;
